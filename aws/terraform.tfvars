# =============================================================
# Fill in your values here
# =============================================================

# Your EC2 key pair name (created in AWS Console → EC2 → Key Pairs)
key_pair_name = "vpn-key"

# Your home public IP for SSH access — find it at https://whatismyip.com
# Format must be x.x.x.x/32
your_home_ip = "172.98.218.0/24"

# Number of VPN client configs to generate (1 per device)
vpn_client_count = 2
